import {
    Store,
    DecimalPrimitive,
    BigIntegerPrimitive,
    FixedSizeBinaryPrimitive,
    Entity,
    BooleanValue,
    Int8Value,
    Int16Value,
    Int32Value,
    Int64Value,
    UInt8Value,
    UInt16Value,
    UInt32Value,
    UInt64Value,
    Float32Value,
    Float64Value,
    Decimal128Value,
    Decimal256Value,
    BigIntegerValue,
    BigDecimalValue,
    Time32Value,
    Time64Value,
    Date32Value,
    Date64Value,
    BinaryValue,
    FixedSizeBinaryValue,
    LargeBinaryValue,
    StringValue,
    LargeStringValue,
    ListValue,
    StructValue,
    _XXX_getEntity,
} from "queryable-sdk";

{{#each entities}}
    {{#ifeq type 'entity'}}
{{> entity }}
    {{else}}
        {{#ifeq type 'struct'}}
{{> struct }}
        {{else}}
            {{#ifeq type 'decimal128'}}
{{> decimal }}
            {{else}}
                {{#ifeq type 'decimal256'}}
{{> decimal }}
                {{else}}
                    {{#ifeq type 'fixedsizebinary'}}
{{> fixedsizebinary }}
                    {{else}}
                      {{#ifeq type 'time32'}}
{{> time }}
                      {{else}}
                          {{#ifeq type 'time64'}}
{{> time }}
                          {{/ifeq}}
                      {{/ifeq}}
                    {{/ifeq}}
                {{/ifeq}}
            {{/ifeq}}
        {{/ifeq}}
    {{/ifeq}}
{{/each}}
