export class {{ name }} extends StructValue {
    constructor(nullable: boolean) {
        super(
            [
            {{#each fields}}
                '{{name}}',
            {{/each}}
            ],
            [
            {{#each fields}}
                {{#if needNullable}}
                new {{{structType}}}({{nullable}}),
                {{else}}
                new {{{structType}}}(),
                {{/if}}
            {{/each}}
            ],
            nullable,
        );
    }

{{#each fields}}
{{> primitiveField fieldInStruct=true }}
{{/each}}
}

